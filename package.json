{
  "name": "people-counter",
  "version": "1.0.0",
  "description": "People counter monorepo - detection, backend, frontend",
  "scripts": {
    "dev": "concurrently --names \"detection,backend,frontend\" --prefix-colors \"yellow,cyan,magenta\" \"npm run dev:detection\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:detection": "cd detection && C:\\pcvenv\\Scripts\\uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npx ng serve"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
